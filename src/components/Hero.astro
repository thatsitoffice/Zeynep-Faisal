---
import BookingButton from "./BookingButton.astro";
import { SITE } from "../data/site";

/**
 * Hero-Bild: Platzhalter — durch eigenes Shooting ersetzen (WebP/AVIF, ca. 1600px breit).
 * Quelle hier nur als hochwertiger Demo-Look; Rechte vor Veröffentlichung klären.
 */
const heroImage = {
  src: "https://images.unsplash.com/photo-1560066984-138dadb4c035?auto=format&fit=crop&w=1600&q=80",
  alt: "Ruhiger, heller Salonraum mit stilvoller Ausstattung — stellvertretend für die Atmosphäre bei New Style",
  width: 1600,
  height: 2000,
};
---

<section class="hero" aria-labelledby="hero-heading">
  <div class="hero__ambient" aria-hidden="true"></div>
  <div class="hero__grid container">
    <div class="hero__copy" data-reveal style="--reveal-delay: 0ms">
      <p class="eyebrow">{SITE.city} · {SITE.tagline}</p>
      <h1 id="hero-heading" class="display-xl">
        Wo Präzision auf Ruhe trifft — Ihr Haar in Meisterhand.
      </h1>
      <p class="lead hero__lead">
        Im Herzen von Gelsenkirchen verwandeln wir Ihren Besuch in ein klares, hochwertiges Erlebnis: Beratung mit Tiefe,
        Handwerk mit Haltung — und ein Ergebnis, das sich jeden Tag leicht tragen lässt.
      </p>
      <div class="hero__actions">
        <BookingButton />
        <a class="btn btn--ghost" href="#leistungen">Leistungen entdecken</a>
      </div>
      <p class="hero__note muted">
        Online buchen bei Planity — Wunschtermin, Service und Team wählen Sie selbst.
      </p>
    </div>
    <figure class="hero__figure" data-reveal style="--reveal-delay: 140ms">
      <div class="hero__frame">
        <img
          src={heroImage.src}
          alt={heroImage.alt}
          width={heroImage.width}
          height={heroImage.height}
          loading="eager"
          fetchpriority="high"
          decoding="async"
          class="hero__img"
        />
      </div>
      <figcaption class="hero__caption">Atmosphäre &amp; Handwerk — authentisches Bild nach Freigabe einsetzen.</figcaption>
    </figure>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding-block: var(--space-xl) var(--space-2xl);
    padding-top: calc(var(--space-xl) + var(--header-h));
    margin-top: calc(-1 * var(--header-h));
  }

  .hero__ambient {
    position: absolute;
    inset: 0;
    top: 10%;
    background: radial-gradient(ellipse 55% 45% at 85% 25%, rgba(196, 165, 116, 0.11), transparent 70%);
    pointer-events: none;
    z-index: 0;
  }

  .hero__grid {
    position: relative;
    z-index: 1;
    display: grid;
    gap: var(--space-xl);
    align-items: center;
  }

  @media (min-width: 960px) {
    .hero__grid {
      grid-template-columns: 1fr 1.05fr;
      gap: var(--space-2xl);
    }
  }

  .hero__lead {
    margin-top: var(--space-md);
    margin-bottom: var(--space-lg);
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
    margin-bottom: var(--space-md);
  }

  .hero__note {
    font-size: 0.875rem;
    margin: 0;
    max-width: 28rem;
  }

  .hero__figure {
    margin: 0;
  }

  .hero__frame {
    position: relative;
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-soft);
    aspect-ratio: 4 / 5;
    background: var(--color-line);
    outline: 1px solid rgba(255, 255, 255, 0.5);
    outline-offset: -1px;
  }

  .hero__frame::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(160deg, transparent 55%, rgba(20, 19, 18, 0.12) 100%);
    pointer-events: none;
    z-index: 1;
  }

  .hero__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    transform: scale(1.07);
    transition: transform 1.35s var(--ease-luxe);
  }

  .hero__figure.is-visible .hero__img {
    transform: scale(1);
  }

  @media (prefers-reduced-motion: reduce) {
    .hero__img {
      transform: none;
      transition: none;
    }
  }

  .hero__caption {
    margin-top: var(--space-sm);
    font-size: 0.75rem;
    color: var(--color-ink-soft);
    line-height: 1.4;
  }
</style>
