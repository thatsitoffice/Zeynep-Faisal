---
---
<section class="hero" id="hero">
  <p class="hero-sub">welcome to your personal sleep salon and massage. your new weekly ritual of relaxing the body and mind.</p>
  <h1 class="hero-title">kalm moments</h1>
  <div class="hero-split">
    <h2>where beauty</h2>
    <div class="hero-image-wrap">
      <div class="hero-image"></div>
    </div>
    <h2>Meets</h2>
  </div>
  <h2 class="hero-nature">Nature</h2>
  <a href="#book" class="hero-cta">book an appointment</a>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 7rem 1.5rem 4rem;
    background: linear-gradient(180deg, var(--color-cream) 0%, var(--color-sand) 100%);
  }

  .hero-sub {
    font-size: 0.95rem;
    color: var(--color-text-muted);
    max-width: 500px;
    margin-bottom: 2rem;
    font-weight: 400;
  }

  .hero-title {
    font-family: var(--font-display);
    font-size: clamp(3rem, 10vw, 6rem);
    font-weight: 300;
    color: var(--color-earth);
    letter-spacing: 0.05em;
    text-transform: lowercase;
    margin-bottom: 2rem;
  }

  .hero-split {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    margin-bottom: 0.5rem;
  }

  .hero-split h2 {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 4vw, 2.5rem);
    font-weight: 400;
    color: var(--color-stone);
  }

  .hero-image-wrap {
    width: 120px;
    height: 80px;
  }

  .hero-image {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, var(--color-sage) 0%, var(--color-stone) 100%);
    border-radius: 8px;
    opacity: 0.7;
  }

  .hero-nature {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 4vw, 2.5rem);
    font-weight: 400;
    color: var(--color-stone);
    margin-bottom: 3rem;
  }

  .hero-cta {
    display: inline-block;
    background: var(--color-stone);
    color: white;
    padding: 1rem 2rem;
    border-radius: 4px;
    font-weight: 500;
    font-size: 0.95rem;
    transition: background 0.2s;
  }

  .hero-cta:hover {
    background: var(--color-earth);
  }
</style>
