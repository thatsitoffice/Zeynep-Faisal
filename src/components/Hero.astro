---
---
<section class="hero" id="hero">
  <div class="hero-image-wrap">
    <img src="/hero-zeynep-faisal.png" alt="Zeynep und Faisal – Inhaber des Friseursalons" />
    <div class="hero-overlay"></div>
  </div>
  <div class="hero-content">
    <p class="hero-sub">Willkommen bei Zeynep & Faisal – Ihrem Friseursalon mit Herz</p>
    <h1 class="hero-title">Zeynep & Faisal</h1>
    <p class="hero-tagline">
      Wir verbinden Leidenschaft für Haardesign mit individueller Beratung. 
      Entdecken Sie unseren Salon – modern, persönlich und mit dem Blick für Ihr bestes Ich.
    </p>
    <a href="https://www.planity.com/de-DE/new-style-by-zeynep-faisal-meisterbetrieb-45891-gelsenkirchen" target="_blank" rel="noopener noreferrer" class="hero-cta">Termin buchen</a>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    overflow: hidden;
  }

  .hero-image-wrap {
    position: absolute;
    inset: 0;
  }

  .hero-image-wrap img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      90deg,
      rgba(44, 36, 32, 0.85) 0%,
      rgba(44, 36, 32, 0.5) 50%,
      rgba(44, 36, 32, 0.2) 100%
    );
  }

  .hero-content {
    position: relative;
    z-index: 1;
    max-width: 560px;
    padding: 6rem 1.5rem 4rem 1.5rem;
    padding-left: 20%;
  }

  .hero-sub {
    font-size: 0.95rem;
    color: var(--color-sand);
    margin-bottom: 0.75rem;
    font-weight: 400;
    opacity: 0.95;
  }

  .hero-title {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 7vw, 4.5rem);
    font-weight: 400;
    color: white;
    letter-spacing: 0.02em;
    margin-bottom: 1.25rem;
    line-height: 1.1;
  }

  .hero-tagline {
    font-size: 1.05rem;
    color: rgba(245, 240, 232, 0.9);
    line-height: 1.7;
    margin-bottom: 2rem;
  }

  .hero-cta {
    display: inline-block;
    background: var(--color-stone);
    color: white;
    padding: 1rem 2rem;
    border-radius: 4px;
    font-weight: 500;
    font-size: 0.95rem;
    transition: background 0.2s;
  }

  .hero-cta:hover {
    background: var(--color-earth);
  }

  @media (max-width: 768px) {
    .hero-overlay {
      background: linear-gradient(
        180deg,
        rgba(44, 36, 32, 0.4) 0%,
        rgba(44, 36, 32, 0.85) 100%
      );
    }

    .hero-content {
      padding-top: 50vh;
      padding-left: 1.5rem;
    }

    .hero-title {
      font-size: clamp(2rem, 9vw, 3rem);
    }

    .hero-tagline {
      font-size: 0.95rem;
    }
  }
</style>
