---
const services = [
  {
    title: "Haarschnitt",
    desc: "Ob klassisch oder modern – wir schneiden Ihr Haar präzise und passend zu Ihrem Typ. Für Damen und Herren.",
    packages: [
      { name: "Damenhaarschnitt", duration: "ca. 45 Min.", price: "ab 45 €" },
      { name: "Herrenhaarschnitt", duration: "ca. 30 Min.", price: "ab 28 €" },
      { name: "Kinderschnitt", duration: "ca. 30 Min.", price: "ab 22 €" },
    ],
    link: "/services/hair-spa",
  },
  {
    title: "Färben & Strähnchen",
    desc: "Neue Nuancen, sanfte Highlights oder kräftige Coloration – wir beraten Sie individuell für Ihren perfekten Look.",
    packages: [
      { name: "Strähnchen", duration: "ca. 90 Min.", price: "ab 75 €" },
      { name: "Komplettfärbung", duration: "ca. 60 Min.", price: "ab 55 €" },
      { name: "Ansatz auffrischen", duration: "ca. 45 Min.", price: "ab 45 €" },
    ],
    link: "/services/body-massage",
  },
  {
    title: "Styling & Pflege",
    desc: "Professionelle Pflegebehandlungen und Styling für gesundes, glänzendes Haar. Verwöhnen Sie sich.",
    packages: [
      { name: "Spülung & Styling", duration: "ca. 30 Min.", price: "ab 25 €" },
      { name: "Haar-Maske", duration: "ca. 45 Min.", price: "ab 35 €" },
      { name: "Hochsteckfrisur", duration: "nach Aufwand", price: "auf Anfrage" },
    ],
    link: "/services/face-massage",
  },
];
---
<section class="services" id="ourServices">
  <h2 class="section-title">Unsere Leistungen</h2>
  <p class="section-desc">Von klassischem Haarschnitt bis zu Farbe und Pflege – wir bieten Ihnen ein volles Programm an Friseurleistungen. Individuell auf Sie abgestimmt.</p>
  <div class="services-grid">
    {services.map((service) => (
      <article class="service-card">
        <h3>{service.title}</h3>
        <p class="service-desc">{service.desc}</p>
        <ul class="packages">
          {service.packages.map((pkg) => (
            <li>
              <span class="pkg-name">{pkg.name}</span>
              <span class="pkg-meta">{pkg.duration} · {pkg.price}</span>
            </li>
          ))}
        </ul>
        <a href={service.link} class="learn-more">Mehr erfahren</a>
      </article>
    ))}
  </div>
</section>

<style>
  .services {
    padding: 5rem 1.5rem;
    background: var(--color-sand);
  }

  .section-title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 5vw, 3rem);
    color: var(--color-earth);
    text-align: center;
    margin-bottom: 1rem;
  }

  .section-desc {
    text-align: center;
    max-width: 600px;
    margin: 0 auto 3rem;
    color: var(--color-text-muted);
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    max-width: 1100px;
    margin: 0 auto;
  }

  .service-card {
    background: white;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.04);
  }

  .service-card h3 {
    font-family: var(--font-display);
    font-size: 1.5rem;
    color: var(--color-earth);
    margin-bottom: 1rem;
  }

  .service-desc {
    color: var(--color-text-muted);
    font-size: 0.95rem;
    margin-bottom: 1.5rem;
    line-height: 1.6;
  }

  .packages {
    list-style: none;
  }

  .packages li {
    padding: 0.5rem 0;
    border-bottom: 1px solid rgba(139, 115, 85, 0.1);
  }

  .packages li:last-child {
    border-bottom: none;
  }

  .pkg-name {
    display: block;
    font-weight: 500;
    color: var(--color-text);
  }

  .pkg-meta {
    font-size: 0.85rem;
    color: var(--color-text-muted);
  }

  .learn-more {
    display: inline-block;
    margin-top: 1.5rem;
    color: var(--color-stone);
    font-weight: 500;
    transition: color 0.2s;
  }

  .learn-more:hover {
    color: var(--color-earth);
  }
</style>
