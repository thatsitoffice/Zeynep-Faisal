---
const testimonials = [
  { author: "Sarah M.", text: "Super nettes Team und tolle Beratung! Bin jedes Mal zufrieden und komme gerne wieder." },
  { author: "Thomas K.", text: "Endlich ein Friseur, der versteht, was ich will. Sehr professionell und unkompliziert." },
  { author: "Lisa B.", text: "Die Strähnchen sind perfekt geworden. Ich habe viele Komplimente bekommen – vielen Dank!" },
  { author: "Michael R.", text: "Bester Herrenhaarschnitt seit langem. Guter Preis, freundlicher Service, top Ergebnis." },
  { author: "Julia S.", text: "Die Atmosphäre im Salon ist so angenehm. Man fühlt sich sofort willkommen." },
  { author: "David W.", text: "Immer pünktlich, immer professionell. Sehr empfehlenswert." },
];
---
<section class="testimonials" id="Gallery">
  <h2 class="section-title">Was unsere Kunden sagen</h2>
  <div class="testimonials-slider">
    <div class="testimonials-track">
      {[...testimonials, ...testimonials].map((t) => (
        <blockquote class="testimonial">
          <p>„{t.text}"</p>
          <cite>— {t.author}</cite>
        </blockquote>
      ))}
    </div>
  </div>
</section>

<style>
  .testimonials {
    padding: 5rem 1.5rem;
    overflow: hidden;
  }

  .testimonials .section-title {
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    color: var(--color-earth);
    text-align: center;
    margin-bottom: 3rem;
  }

  .testimonials-slider {
    overflow: hidden;
  }

  .testimonials-track {
    display: flex;
    gap: 2rem;
    animation: scroll 40s linear infinite;
  }

  @keyframes scroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  .testimonial {
    flex: 0 0 350px;
    background: white;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.04);
    font-style: normal;
  }

  .testimonial p {
    font-size: 1rem;
    color: var(--color-text);
    margin-bottom: 1rem;
    line-height: 1.6;
  }

  .testimonial cite {
    font-size: 0.9rem;
    color: var(--color-text-muted);
    font-style: normal;
  }
</style>
