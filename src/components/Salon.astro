---
---
<section class="salon" id="salon">
  <div class="salon-inner">
    <div class="salon-content">
      <span class="salon-label">Unser Salon</span>
      <h2 class="salon-title">Wo Stil auf Ruhe trifft</h2>
      <p class="salon-text">
        Helle Räume, dezentes Schwarz-Weiß und natürliches Licht – unser Salon verbindet 
        zeitgemäßes Design mit einer Atmosphäre, in der Sie sich von der ersten Minute an 
        wohlfühlen.
      </p>
      <ul class="salon-features">
        <li>Große Spiegel & professionelle Arbeitsplätze</li>
        <li>Hochwertige Produkte & Tools</li>
        <li>Natürliches Tageslicht</li>
      </ul>
    </div>
    <div class="salon-image">
      <img 
        src="/salon-interior.png" 
        alt="Unser Salon – modernes Ambiente mit Lederstühlen, Spiegeln und natürlichem Licht" 
        loading="lazy"
      />
    </div>
  </div>
</section>

<style>
  .salon {
    padding: var(--section-padding) clamp(1.5rem, 5vw, 3rem);
    background: var(--color-sand);
  }

  .salon-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(2rem, 5vw, 4rem);
    max-width: 1100px;
    margin: 0 auto;
    align-items: center;
  }

  .salon-content {
    order: 1;
  }

  .salon-image {
    order: 2;
    position: relative;
    aspect-ratio: 4/3;
    overflow: hidden;
    border-radius: 8px;
  }

  .salon-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .salon-label {
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--color-stone);
    margin-bottom: 0.75rem;
  }

  .salon-title {
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 3.5vw, 2.25rem);
    font-weight: 400;
    color: var(--color-earth);
    line-height: 1.2;
    margin-bottom: 1.25rem;
  }

  .salon-text {
    font-size: 1.1rem;
    color: var(--color-text-muted);
    line-height: 1.9;
    margin-bottom: 1.25rem;
  }

  .salon-features {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .salon-features li {
    position: relative;
    padding: 0.4rem 0 0.4rem 1.5rem;
    color: var(--color-text);
    font-size: 0.95rem;
  }

  .salon-features li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0.85em;
    width: 6px;
    height: 6px;
    background: var(--color-sage);
    border-radius: 50%;
  }

  @media (max-width: 768px) {
    .salon-inner {
      grid-template-columns: 1fr;
    }

    .salon-content {
      order: 1;
    }

    .salon-image {
      order: 2;
      aspect-ratio: 16/10;
    }
  }
</style>
